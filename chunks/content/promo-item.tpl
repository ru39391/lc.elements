<div class="promo__list-item js-tabs-pane {$mod}" data-id="promo-tab-{$idx}">
    <div class="promo__list-title">{$title}</div>
    <div class="promo__list-picture">{if $img}<img class="promo__list-img" src="{$img}" alt="{$title}" />{/if}</div>
    <div class="promo__list-content content">{$content}</div>
</div>